version: '1'

defaults:
  simulator: icarus
  pass_fail:
    type: string_match
    pass_string: TEST PASSED
  waveform:
    capture: never

suites:
  smoke:
    description: Single-test smoke suite for the iverilog driver fixture.
    tests:
      - name: pass_basic
        top: tb_pass
        sources:
          - tb_pass.v
