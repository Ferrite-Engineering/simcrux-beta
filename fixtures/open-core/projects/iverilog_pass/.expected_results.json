{
  "description": "Expected results for the iverilog_pass fixture. Tests load the corresponding simcrux.yaml through ConfigLoader and assert the regression-runner produces one TestResult whose status matches `expected_status`.",
  "tests": [
    {
      "id": "smoke/pass_basic",
      "expected_status": "pass"
    }
  ]
}
